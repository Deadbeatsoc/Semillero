# Puerto en el que se ejecuta el servidor Express
PORT=4000

# URL del servicio de ArcGIS que expone las predicciones de accidentes
ARCGIS_PREDICTIONS_URL=https://services.arcgis.com/tu-organizacion/arcgis/rest/services/predicciones/FeatureServer/0/query

# Autenticación con ArcGIS (usa el método que prefieras)
# Opción 1: token OAuth2 generado con client credentials
ARCGIS_CLIENT_ID=tu_client_id
ARCGIS_CLIENT_SECRET=tu_client_secret

# Opción 2: token listo para usar. Si lo defines no necesitas client id/secret
# ARCGIS_TOKEN=tu_token_de_acceso

# Opción 3: credenciales básicas (solo si el servicio las soporta)
# ARCGIS_USERNAME=usuario
# ARCGIS_PASSWORD=contrasena
